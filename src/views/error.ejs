<%- include('partials/head', { pageTitle: 'Error', bodyClass: 'error-page' }) %>

    <div class="error-card">
        <span class="error-card__icon">⚠️</span>
        <h1 class="error-card__title">Something went wrong</h1>
        <div class="error-card__message">
            <%= typeof message !== 'undefined' ? message : 'An unexpected error occurred.' %>
        </div>
        <div class="error-card__actions">
            <a href="/" class="btn btn-primary">🏠 Go to Dashboard</a>
            <button onclick="window.history.back()" class="btn btn-secondary">← Go Back</button>
        </div>
    </div>

</body>
</html>
